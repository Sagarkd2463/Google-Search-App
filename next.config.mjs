/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: '',
                hostname: '',
                pathname: '',
            },
        ],
    },
};

export default nextConfig;
